# 공동보상 초점 보상함수 설계 분석

## 문제 분석: 왜 음의 보상이 폭발하는가?

### 1. 환경 설정의 불 역학
```python
ENV_CONFIG:
- Grid size: 22x22
- Agents: 6개
- Max steps: 300
- Fire dynamics: alpha=0.05, delta_beta=0.54
```

**불의 특성:**
- `alpha=0.05`: 매 스텝마다 5% 확률로 이웃 나무에 불 확산
- `delta_beta=0.54`: 불의 번아웃/감소 속도는 상대적으로 느림
- **결과**: 불이 **계속 새로 번져나감** (T_n이 지속적으로 증가)

### 2. 원본 individual_reward()의 문제점

```python
# Original coefficients
R_i = (1.0 * T_e_i) + 0.1 * (1.0 * T_e - 0.3 * T_n - 0.5 * T_b)

Where:
- T_e_i: 에이전트 i가 진화한 나무 수 (개별 기여)
- T_e: 전체 에이전트가 진화한 총 나무 수 (공동)
- T_n: 새로 불붙은 나무 수
- T_b: 새로 탄 나무 수
```

**구조적 문제:**
1. **공동보상 비율이 너무 낮음 (10%)**
   - 개별 기여 90% vs 공동 협력 10%
   - 협력 인센티브 미약

2. **음의 신호 폭발**
   ```
   Shared reward = 1.0 * T_e - 0.3 * T_n - 0.5 * T_b

   일반적인 시나리오 (하나의 스텝):
   - T_e = 2 (2개 나무 진화)
   - T_n = 5 (5개 새로 불붙음)
   - T_b = 1 (1개 탐)

   Shared reward = 1.0*2 - 0.3*5 - 0.5*1
                 = 2 - 1.5 - 0.5 = 0 (중립)
   ```

   **공동보상 비율을 0.1에서 0.6으로 올리면:**
   ```
   R_i = 0.9 * (1.0 * T_e_i) + 0.6 * (shared_reward)

   같은 상황에서:
   - shared_reward이 음수면, 그 음수가 0.6배가 되어 절댓값이 6배 증가
   - 학습 불안정성 심화
   ```

3. **에이전트 수에 비례하는 폭발**
   - 6명의 에이전트가 각각 shared_reward를 받음
   - 음의 신호가 6명 모두에게 동등하게 전파되어 음의 강화 증가

---

## 해결책: cooperative2_reward

### 설계 원칙

#### 1️⃣ 공동보상 비율 증가 (10% → 50%)
```python
r_shared = 0.5  # 50% 협력 초점
```
- 원본보다 5배 증가했지만, 안정성을 위해 다른 조정과 병행

#### 2️⃣ 페널티 계수 감소 (음의 폭발 제한)
```python
# Original
r_new_fire: 0.3  → cooperative2: 0.15 (50% 감소)
r_new_burnt: 0.5 → cooperative2: 0.25 (50% 감소)

# 효과:
Shared reward = 1.0 * T_e - 0.15 * T_n - 0.25 * T_b
              = 2 - 0.75 - 0.25 = 1.0 (동일 시나리오)
```

**의미:**
- 불 확산에 대한 음의 신호를 절반으로 경감
- 절댓값 크기의 폭발 방지
- 동시에 보상의 절댓값도 줄어들어 스케일 균형

#### 3️⃣ Shared reward 정규화 (에이전트 수 대응)
```python
if normalize_shared and num_agents > 0:
    shared_reward = shared_reward / num_agents  # 6으로 나누기

# 효과:
- 6명의 에이전트가 각각 1/6배의 shared_reward 수신
- 에이전트 수가 많아도 보상 폭발 방지
- 개인이 큰 팀에서 받는 부담 경감
```

---

### 수학적 비교

**원본 (Individual reward)**
```
R_i = 1.0 * T_e_i + 0.1 * (1.0 * T_e - 0.3 * T_n - 0.5 * T_b)
    = Individual(90%) + SharedUnscaled(10%)
```

**개선안 (Cooperative2 reward)**
```
R_i = 0.5 * [(1.0 * T_e - 0.15 * T_n - 0.25 * T_b) / 6]
    + 0.5 * (1.0 * T_e_i)
    = SharedNormalized(50%) + Individual(50%)
```

**구체적 예시 (1 스텝):**
```
T_e = 2, T_n = 5, T_b = 1, T_e_agent_i = 0 (에이전트 i가 안 함)

Original:
R_i = 1.0*0 + 0.1*(1.0*2 - 0.3*5 - 0.5*1)
    = 0 + 0.1 * 0
    = 0.0 (불안정한 신호)

Cooperative2:
Shared = (1.0*2 - 0.15*5 - 0.25*1) / 6
       = (2 - 0.75 - 0.25) / 6
       = 1.0 / 6 = 0.167

R_i = 0.5 * 0.167 + 0.5 * 1.0*0
    = 0.083 (약한 긍정 신호)
```

---

## 학습 효과 예측

### Original vs Cooperative2

| 측면 | Original | Cooperative2 | 효과 |
|------|----------|-------------|------|
| **공동보상 비율** | 10% | 50% | ✅ 협력 인센티브 5배 증가 |
| **페널티 절댓값** | 크다 | 반으로 감소 | ✅ 음의 폭발 방지 |
| **에이전트당 신호** | 공유 X 정규화 | /num_agents | ✅ 팀 크기에 강건 |
| **개별 기여 인센티브** | 90% | 50% | ⚠️ 약간 감소 (필요함) |
| **신호 크기 범위** | 불안정 (-5 ~ +5) | 안정적 (-0.5 ~ +1) | ✅ 학습 안정화 |

---

## 환경.py에서 사용하는 방법

```python
# environment.py
ENV_CONFIG = {
    ...
    "reward_shaping": "cooperative2",  # "individual" → "cooperative2"로 변경
    "reward_shaping_config": None,  # 기본값 사용
}
```

### 필요시 미세 조정

```python
# reward_shaping_config를 사용하여 런타임에 파라미터 변경 가능
"reward_shaping_config": {
    "r_extinguish": 1.0,
    "r_new_fire": 0.15,  # 필요시 0.1로 더 낮추기 가능
    "r_new_burnt": 0.25,
    "r_shared": 0.6,  # 협력 비율 더 높이기 (0.5 → 0.6)
    "normalize_shared": True,
}
```

---

## 추가 튜닝 가이드

### 1. 음의 보상이 여전히 크면
- `r_new_fire`를 0.15에서 0.1로 감소
- `r_new_burnt`를 0.25에서 0.15로 감소

### 2. 협력이 충분하지 않으면
- `r_shared`를 0.5에서 0.6 또는 0.7로 증가
- **단, 페널티 계수도 함께 감소시켜야 함**

### 3. 개별 성과를 더 강조하려면
- `r_shared`를 0.5에서 0.4로 감소
- 하지만 너무 낮추면 원래 문제로 돌아감

---

## 핵심 요점

1. ✅ **페널티 계수 감소가 가장 중요**: 음의 신호를 절반으로 경감
2. ✅ **정규화를 통한 스케일 제어**: `/num_agents`로 에이전트 수 대응
3. ✅ **공동보상 비율 증가**: 협력 인센티브 강화 (하지만 급격하지 않게)
4. ✅ **이 세 가지의 조합**: 안정적인 학습 + 협력 유도

