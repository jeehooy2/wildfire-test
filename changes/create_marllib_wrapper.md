MARLlib에서 커스텀 환경을 추가하는 방법에 대한 add_new_env.py를 참고해서 다음을 실행해줘. (모든 변화는 train_marllib_self 폴더 내에서만)
1) wildfire_rllib_wrapper.py의 역할을 동일하게 수행하되, MARLlib에 적합한 wildfire_marllib_wrapper.py 파일을 만들어줘.
2) train_rllib_mappo_new.py의 역할을 동일하게 수행하되, MARLlib에 적합한 train_marllib_mappo_new.py 파일을 만들어줘.